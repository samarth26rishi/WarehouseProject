# Person C — Frontend

Dashboard, video player + overlays, event timeline, filters, chat UI, alerts.
Talks only to the FastAPI endpoints in backend/main.py — no direct DB/SQL access.

Not started yet in this scaffold — set up your React (or Streamlit) app here.
